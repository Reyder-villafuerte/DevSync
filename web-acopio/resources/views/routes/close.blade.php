@extends('layouts.app',['title'=>'Cerrar Ruta y Descargar'])
@section('content')<div class="page-header"><h1>Cerrar Ruta y Descargar</h1></div>@forelse($routes as $route)<div class="mf-card"><h2>{{ $route->nombre }} · {{ $route->vehiculo }}</h2><p>{{ $route->sectores->pluck('nombre')->join(' · ') }}</p><p>Litros recogidos hoy: <b>{{ $route->entregas()->whereDate('fecha_hora',today())->sum('litros') }} L</b></p><p class="muted">El cierre consolida la descarga en planta. Después del cierre no se admiten nuevas entregas para esta ruta durante el día.</p><form method="POST">@csrf<input type="hidden" name="ruta_id" value="{{ $route->id }}"><button class="mf-button">CERRAR RUTA Y DESCARGAR CSV</button></form></div>@empty<div class="mf-card empty">No tienes una ruta activa asignada.</div>@endforelse @endsection
