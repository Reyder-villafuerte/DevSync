@extends('layouts.app',['title'=>'Sincronización'])
@section('content')<div class="page-header"><h1>Sincronización</h1>@if(auth()->user()->hasRole('acopiador'))<a href="/acopiador/offline" class="mf-button">Abrir recolección sin conexión</a>@endif</div><div class="mf-card"><h2>Registros enviados al servidor</h2><p class="muted">Cada entrega conserva su UUID al reenviarse. Los registros pendientes del dispositivo se consultan desde la recolección sin conexión.</p><table><thead><tr><th>UUID</th><th>Estado</th><th>Fecha</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->uuid }}</td><td><span class="mf-badge">{{ $record->estado }}</span></td><td>{{ $record->created_at }}</td></tr>@empty<tr><td colspan="3" class="empty">Sin registros pendientes en el servidor.</td></tr>@endforelse</tbody></table>{{ $records->links() }}</div>@endsection
